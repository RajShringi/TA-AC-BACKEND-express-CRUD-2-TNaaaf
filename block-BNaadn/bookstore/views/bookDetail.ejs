<!DOCTYPE html>
<html>
  <head>
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body class="text-gray-500">
    <div class="container mx-auto">
      <h1><%= book.title %></h1>
      <p><%= book.category.join(' ') %></p>
      <p><%= book.summary %></p>
      <p><%= book.author %></p>
      <p class="my-4"><%= book.publication %></p>
      <a
        href="/books/<%= book.id %>/edit"
        class="px-4 py-2 bg-red-400 rounded text-white inline-block"
        >Edit</a
      >
      <a
        href="/books/<%= book.id %>/delete"
        class="px-4 py-2 bg-red-400 rounded text-white inline-block"
        >Delete</a
      >
    </div>
  </body>
</html>
